<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:id="@+id/RL_timeTableItem"
    >

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/textClockTo"
        android:layout_alignParentTop="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:visibility="invisible"
        />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/to"
        android:layout_alignBaseline="@id/textClockTo"
        android:layout_toLeftOf="@id/textClockTo"
        android:layout_toStartOf="@id/textClockTo"
        android:text="@string/workingHItem_textViewTo"
        android:visibility="invisible"
        />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/textClockFrom"
        android:layout_alignParentTop="true"
        android:layout_toLeftOf="@id/to"
        android:layout_toStartOf="@id/to"
        android:visibility="invisible"
        />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/from"
        android:layout_toLeftOf="@id/textClockFrom"
        android:layout_toStartOf="@id/textClockFrom"
        android:layout_alignBaseline="@id/textClockFrom"
        android:text="@string/workingHItem_textViewFrom"
        android:visibility="invisible"
        />

    <CheckBox
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/checkBox"
        android:layout_alignParentTop="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_alignBaseline="@id/textClockFrom"
        />

</RelativeLayout>